// Windows resource: the application icon shown by Explorer, the taskbar, and the
// title bar. Explorer uses the numerically lowest ICON resource for the .exe, so
// this uses id 1. Path is relative to this .rc file (CMake compiles it in place).
1 ICON "../icon/RegionRecord.ico"
